# 나중에 다시풀어보자 ;;
import heapq

def solution(n, start, end, roads, traps):
    answer = 0
    return answer

n, start, end = 3, 1, 3
roads = [[1, 2, 2], [3, 2, 3]]
traps = [2]
print(solution(n, start, end, roads, traps))